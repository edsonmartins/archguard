# DIVERGENCE.md — Registro de divergência estrutural em relação ao upstream

> Exigido pelo ADR-0003. Todo desvio estrutural entra aqui com subsistema e motivo; a triagem
> de cherry-pick cruza cada commit do upstream com esta tabela. Commit que toque subsistema
> divergente exige revisão manual.

## Topologia do fork (nota permanente)

`main` nasceu do corpus de governança e recebeu a árvore do fork point (`v3.119.0` =
`50e77ade`) por **um único merge de bootstrap** no T-002, autorizado em 2026-07-20. Esse merge
existe para dar a `main` ancestralidade com o upstream — é o que fornece base de merge aos
cherry-picks do ADR-0003. Ele é o ato de criação do fork, não sincronismo: **a proibição de
merge do ADR-0003 vale integralmente daqui em diante** — importação futura é exclusivamente
cherry-pick com trailer `Upstream-Commit: <sha>`.

## Ferramenta de triagem (ADR-0003)

`make upstream-triage` (código em `tools/upstreamwatch`) atualiza o espelho `vendor/upstream` e
emite a fila de commits novos desde `docs/upstream/LAST_SYNC.md`, classificados por natureza e
cruzados com os prefixos divergentes/removidos registrados nesta tabela. **Ao adicionar uma
divergência aqui, considere refletir o prefixo em `tools/upstreamwatch/classify.go`** para que a
triagem futura marque corretamente os commits que a tocam.

## Divergências

| Data | Subsistema / caminho | Divergência | Motivo | Referência |
|---|---|---|---|---|
| 2026-07-24 | `controllers/system_info.go` (editado), `internal/domain/compliance.go` (**novo**) | **Health check de conformidade por CUSTÓDIA (pacote 010, T-016 / spec "Custódia local em produção")**. O `/health` deixa de reportar conformidade só pelo rótulo do perfil e passa a chavear pela CUSTÓDIA (`domain.ComplianceReport`): custódia local (keystore selado) é SEMPRE não conforme, mesmo fora do perfil dev — o material privado deve viver no cofre (ADR-0012). Adiciona `nonConformReasons`. Refina a divergência do ADR-0017 (mesma rota `/health`) | ADR-0012, ADR-0017, pacote 010 (T-016) | pacote 010 (T-016) |
| 2026-07-24 | `main.go` (editado), `internal/domain/legacy_channel.go` (**novo**) | **Servidor RADIUS embutido desabilitado por padrão (pacote 009, T-014 / RFC-0007 §6, I-4.4)**. O upstream inicia `radius.StartRadiusServer()` INCONDICIONALMENTE; agora só sobe quando `enableRadiusServer` é um afirmativo explícito (`domain.LegacyChannelConfig`, leitura conservadora — ausente/vazio/`false`/malformado ⇒ desligado). É o único canal legado de borda que resta (o servidor LDAP embutido já foi removido no ADR-0019); o conector cliente LDAP/AD (T-002) não é afetado. **Impacto de triagem**: commit do upstream que toque a inicialização do RADIUS é avaliado contra este gate | RFC-0007 §6, ADR-0019, I-4.4 | pacote 009 (T-014) |
| 2026-07-23 | `internal/domain/oidc_*.go` (claims/flow/audience/introspection/logout/client/openbao/guacamole_edge), `internal/domain/refresh_token.go`, `internal/adapters/oidc/**` (signer/JWKS/conformance), `internal/adapters/postgres/{refresh_token_store,refresh_exchanger,session_revoker}.go`, `internal/migrate/migrations/0029`, `Makefile` (release-gate/conformance), `docs/oidc/**` (**novos/estendidos**); semanticamente: `object/token_jwt.go` + controllers OIDC do Casdoor | **Federação OIDC do ArchGate (pacote 006, RFC-0006/ADR-0011 — COMPLETO, 20/20)**. O JWT/claims default do Casdoor (`object/token_jwt.go`) é SUPERADO por um contrato de claims v1 VERSIONADO e agnóstico de fornecedor (`domain.OIDCClaims`): org/mid/acr/amr/sid/auth_time + act (delegação 004)/pcid (correlação de sessão privilegiada)/grant_ref, e-mail só sob escopo (I-3.2). Vocabulário de acr reconciliado a L1/L2/L3 (`AAL.Level()`, fonte única — token, desafio de step-up e auditoria não divergem). Fluxos: PKCE obrigatório, implicit/ROPC recusados por construção, audiência por componente (token de A recusado por B), escopo mínimo, L3 bloqueado em device flow. Ciclo de token: refresh em FAMÍLIA com rotação obrigatória e detecção de reuso (`RefreshExchanger` → revoga família + `token.refresh.reuse` severidade alta + alerta crítico; migração 0029, só hash INV-7). Signer RS256 + JWKS com kid e rotação por sobreposição (`internal/adapters/oidc`, golang-jwt/v5 + go-jose/v4 já no tree — SEM dep nova). Back-channel logout (fail-closed local + envios best-effort reportados) + introspecção de TTL curto para quem não tem logout. 5 clientes registrados (Warpgate/Guacamole/NetBird/OpenBao/proxy-Oracle) com audiência/fluxos/escopos por perfil; mapa determinístico claims→políticas do OpenBao (mesma fonte de papéis); borda documentada do Guacamole (`docs/oidc/GUACAMOLE-EDGE.md`). Suíte de CONFORMIDADE por componente (`conformance_test.go`) + `make release-gate`/`make conformance` (gate de release, I-9.4). **Impacto de triagem**: commit do upstream que toque `object/token_jwt.go` ou os controllers OIDC é avaliado contra o contrato novo | ADR-0011, RFC-0006, I-3.2/9.4 | pacote 006 (T-001..T-020, COMPLETO) |
| 2026-07-23 | `internal/domain/{privileged_grant,delegation,breakglass_service,post_use_review,notification,identity,audit_event,operation_catalog}.go`, `internal/adapters/postgres/{privileged_grant_store,grant_expirer,breakglass_orchestrator,privileged_access_service}.go` + `auth_session_store.go` (RevokeByGrant), `internal/migrate/migrations/0027..0028`, `test/invariants/inv8_*` (**novos/estendidos**); semanticamente: login/impersonation legados do Casdoor | **Controles de acesso privilegiado (pacote 004, ADR-0008 — COMPLETO, 20/20)**. A impersonation com log insuficiente do upstream é SUPERADA por delegação explícita: `Delegation` nasce `pending_consent` (consentimento é o padrão), `TokenClaims` emite `sub` (impersonado) + `act` (ator real, RFC 8693) só se ativa/vigente, `AuditActor()` grava AMBOS (não-repúdio 100%), banner permanente, revogação por alvo/admin, escopo REDUZIDO (`ForbiddenUnderDelegation` no catálogo + `DelegationScopeGuard`: sem admin/segredo/aprovação), conta de serviço NUNCA impersonável. Break-glass formal (`PrivilegedGrant` + máquina de estados): justificativa+incidente obrigatórios, step-up WebAuthn recusando TOTP, N pares DISTINTOS sem autoaprovação (zero proibido em produção), alerta em tempo real NA SOLICITAÇÃO, fail-closed em auditoria E canal (`BreakglassOrchestrator`), expiração no MOMENTO DA DECISÃO (`Authorizes`) + job de materialização com cascata de sessões derivadas (`GrantExpirer`/`RevokeByGrant`), revisão pós-uso obrigatória (pendência visível). Todo o ciclo auditado (`PrivilegedAccessService`, 9 ações novas no catálogo fechado; INV-8 verde). Conta de serviço sem login interativo. Persistência: migrações 0027/0028 (privileged_grant/grant_approval/breakglass_review + link auth_session→grant), RLS FORCE por org. Porto `Notifier` novo (distinto do Alerter, disponibilidade é pré-condição fail-closed). **Sem dependência nova**. **Impacto de triagem**: commit do upstream que toque login/impersonation é avaliado contra o modelo de delegação/break-glass novo | ADR-0008, I-4.1/4.2/5.1 | pacote 004 (T-001..T-020, COMPLETO) |
| 2026-07-20 | `README.md` | README do upstream movido intacto (sem remoção de avisos) para `docs/upstream/README-upstream.md`; raiz passa a ter o índice do corpus de governança | O README raiz é o índice normativo do ArchGuard; atribuição preservada conforme ADR-0002. Cherry-pick futuro que toque `README.md` exige revisão manual | T-002, ADR-0002 |
| 2026-07-20 | `Makefile` | Makefile do upstream movido intacto para `docs/upstream/Makefile-upstream`; raiz passa a ter o Makefile do gate (CLAUDE.md §5) | O gate de verificação é contrato do método (I-9.4); alvos úteis do upstream serão absorvidos em T-019 | T-002, T-000c |
| 2026-07-20 | `.gitignore` | União do `.gitignore` do upstream com entradas do ArchGuard | Preservar padrões de build do upstream reduzindo conflito de cherry-pick | T-002 |
| 2026-07-20 | raiz do repositório | Arquivos de governança adicionados: `CLAUDE.md`, `CONSTITUTION.md`, `docs/adr/`, `docs/rfc/`, `openspec/`, `docs/upstream/` | Método SDD do ArchGuard (I-9.1); inexistentes no upstream, sem sobreposição de caminho | pacote 001 |
| 2026-07-20 | `ldap/` (**removido**), `main.go`, `conf/app.conf`, `go.mod` | **Servidor LDAP embutido removido** com a dependência `lor00x/goldap` (GPL-2.0) e as chaves `ldapServerPort`/`ldapsCertId`/`ldapsServerPort`. O conector **cliente** LDAP/AD (`go-ldap/ldap/v3` em `object/`) e o servidor RADIUS **não** são afetados. Cherry-pick futuro que toque `ldap/` é descartado na triagem | ADR-0019 (Parte III), ADR-0015 §5 | T-010a, ADR-0019 |
| 2026-07-20 | `object/{order,order_pay,payment,plan,pricing,product,subscription}.go` e controllers homônimos (**removidos**); `pp/` (**removido**); `provider.go`, `init_data.go`, `init_data_dump.go`, `invitation.go`, `organization.go`, `ormer.go`, `user.go`, `user_util.go`, `router.go`, `controllers/{auth,account,invitation}.go` (editados) | **Pagamento/produto/assinatura removidos** (ADR-0015 §2). Inclui o gating de subscription no fluxo de auth (`paid-user`), a função `GetPaymentProvider`, o carrinho (`User.Cart`) e os seeds/dump. Reduz INV-4: `hashicorp/go-cleanhttp` (MPL, via Paddle) sai do grafo. **Pendência de esquema:** colunas `cart`, `balance`, `balanceCredit`, `balanceCurrency` ficam órfãs (XORM Sync2 não dropa) até migration explícita pós-T-013 — nunca por auto-sync | ADR-0015 §2 | T-008 |
| 2026-07-20 | `mcpself/`, `mcp/`, `object/{agent,server,openclaw_*}.go`, `log/agent_openclaw.go`, `controllers/{agent,server,server_online,server_sync,mcp_server,entry_opentelemetry,entry_util}.go`, `scan/intranet_server.go`, `routers/mcp_util.go` (**removidos**); `provider.go`, `log_provider.go`, `ormer.go`, `oauth_dcr.go`, `scan/provider.go`, `controllers/{base,entry}.go`, `routers/{base,authz_filter,auto_signin_filter,router}.go` (editados) | **Autenticação de agentes de IA/MCP removida** (ADR-0015 §2): servidor MCP `/api/mcp`, registro de servidores MCP (`Server`/`Tool`), entidade `Agent`, provider `Agent`/OpenClaw (sync de transcrição + telemetria OTLP de agente), scanner de intranet MCP. **Fronteira preservada:** OAuth DCR (RFC 7591, geral — `firstNonEmpty` reancorado em `oauth_dcr.go`), `object/Entry` + framework de log-collector (System/SELinux logs), "Security Scan", e o cliente/servidor RADIUS **ficam**. Sai do grafo: `modelcontextprotocol/go-sdk`, `go.opentelemetry.io/proto/otlp`. Redução de **superfície de ataque**, não de licença (SDK era permissivo) | ADR-0015 §2 | T-009 |
| 2026-07-20 | `notification/{16 provedores}`, `idp/{goth + 23 provedores}` (**removidos**); `notification/provider.go`, `idp/provider.go`, `object/{token_oauth,provider}.go`, `controllers/auth.go`, `routers/router.go` (editados) | **Catálogo curado (ADR-0015 §3)**: notificações reduzidas a Custom HTTP/webhook + Slack/Teams/Google Chat; IdPs reduzidos a Entra/AD (adfs, azuread_b2c), Google, Okta, GitHub, GitLab, Custom (OIDC/OAuth2 genérico), Casdoor. Removido o caminho **goth** (~40 provedores sociais) e os fluxos WeChat (mini-program token, Official Account QR/webhook). Esvazia do baseline: `mautrix`, `go.mau.fi/util`, `mrjones/oauth`. `freetype` (avatar identicon, geral) **fica** com eleição FTL (LICENSE_ELECTIONS.md + NOTICE). Decisão: manter GitHub/GitLab (usuário, 2026-07-20) | ADR-0015 §3 | T-010 |
| 2026-07-20 | `internal/deploy/**`, `internal/adapters/keystore/**` (**novos**); `object/{deployment,token_jwt,saml_idp}.go`, `controllers/system_info.go`, `main.go`, `conf/app.conf` | **Perfis de implantação + keystore selado** (ADR-0017): perfil `dev`/`pilot`/`production` obrigatório no boot; keystore local selado (AES-256-GCM, unseal por env no boot) no perfil dev; a chave de assinatura de token sai do banco para o keystore (DB guarda referência `keystore:<id>`) — **I-4.3 satisfeito para o perfil dev**, roteado em JWT e SAML idp via `CertPrivateKeyPEM`. Travas do dev: aviso, `/health` `compliance:non_conformant`, recusa sob exposição pública, hook de negação L3. Upstream guarda a chave privada em claro no banco | ADR-0017, I-4.3 | T-020a/b/c |
| 2026-07-20 | `internal/domain/**`, `internal/adapters/postgres/**` (**novos**) | **Fronteiras de framework** (ADR-0016): `internal/domain/` (domínio puro, sem Beego/XORM — regra INV-3 ativa no CI) com o primitivo Allowed/Denied/Failed (fail-closed INV-6); `internal/adapters/postgres/` (persistência nova via pgx: `NewPool`, `WithTx` = uma transação por operação de negócio RFC-0002 §5). Estrutura de camadas do CLAUDE.md §6 estabelecida; upstream não tem essa separação | ADR-0016, RFC-0002 | T-015, T-016 |
| 2026-07-20 | `deploy/postgres/roles.sql` (**novo**), `object/ormer.go`, `conf/app.conf` | **Papéis de banco segregados** (ADR-0009, RFC-0003): archguard_migrate/app/readonly. O role da aplicação **não tem UPDATE/DELETE nas tabelas de auditoria** (`record`) — barreira física do INV-2, complementar à barreira de código. `migrationDataSourceName` segrega a conexão de migração (archguard_migrate) da conexão de runtime (archguard_app). Upstream usa role único; ArchGuard segrega por privilégio mínimo. Aplicação do roles.sql validada no smoke test (T-022) | ADR-0009, RFC-0003 | T-014 |
| 2026-07-20 | `internal/migrate/` (**novo**), `object/ormer.go`, `main.go`, `go.mod` | **Framework de migrations versionadas** (ADR-0009/ADR-0016): migrator próprio em `pgx` + `pg_advisory_lock` (concorrência entre réplicas), tabela `schema_migrations`, migrations SQL numeradas embutidas. Migration `0001` dropa as colunas órfãs `cart`/`face_ids`/`master_password` (destrutivo EXPLÍCITO, nunca auto-sync). Roda após o `Sync2` legado (coexistência, RFC-0002 §5). **Dependência nova aprovada:** `github.com/jackc/pgx/v5` v5.10.0 (BSD-3, matriz ADR-0002). Aplicação real da migration validada no smoke test (T-022, PG real) | ADR-0009, ADR-0016 | T-013 |
| 2026-07-20 | `sync/`, `sync_v2/` (**removidos**); `object/{ormer,syncer_database,viaSSHDialer}.go` (editados); `conf/app.conf` | **Dialetos não-PostgreSQL removidos** (ADR-0009): drivers mysql/mssql/sqlite fora de `ormer.go`; backend e syncer de banco externo reduzidos a postgres-only; guarda de inicialização recusa `driverName != postgres`; syncers de replicação MySQL (`sync/`, `sync_v2/` — não importados) removidos com `go-mysql-org/go-mysql`. `app.conf` default → postgres. Esvazia do baseline `go-sql-driver/mysql` (MPL) e `modernc/mathutil` (via sqlite). **Pendente:** cenário "PostgreSQL < 15 recusa iniciar" validado no smoke test (T-022, PG real) | ADR-0009 | T-012 |
| 2026-07-20 | `object/{check,organization,application_util}.go` (editados); `test/invariants/known_violations.txt` (**deletado**) | **Senha-mestra removida** (INV-1 / I-4.1 / ADR-0008): backdoor de autenticação por credencial de organização eliminado de `check.go`; campo `Organization.MasterPassword` + hashing/masking/omit removidos. `known_violations.txt` deletado no mesmo commit (autodestruição, design.md). **INV-1 verde sem allowlist.** Coluna `master_password` não é mais criada em instalação nova; DROP p/ bancos existentes em T-013. Cherry-pick que reintroduza senha-mestra é rejeitado pela suíte de invariantes | INV-1, ADR-0008 | T-011 |
| 2026-07-20 | `faceId/`, `idv/` (**removidos**), `captcha/aliyun.go` (**removido**); `controllers/{face(removido),auth,user}.go`, `object/{user,user_util,provider,verification,application_util,syncer_util}.go`, `captcha/provider.go`, `routers/router.go` (editados) | **Reconhecimento facial + verificação de identidade (KYC Alibaba) removidos** (ADR-0015 §2/§5 — não-PAM): face-signin no login, `VerifyIdentification`, provider faceId/idv, `User.FaceIds`, captcha Aliyun. Esvazia do baseline os 6 `alibabacloud-go/*`. **Pendência de esquema (T-013):** coluna órfã `face_ids` (+ colunas de provedores sociais removidos) por migration explícita, nunca auto-sync | ADR-0015 §2/§5 | T-010 |
| 2026-07-21 | `internal/{domain,adapters/postgres,adapters/{keycustodian,secretstore,globalaccess,tenantswitch}}/**`, `internal/{credmigration,rolemigration,identdedup,identfusion,migrehearsal}/**`, `internal/migrate/migrations/0002..0014`, `test/invariants/inv5_*`, `deploy/postgres/roles.sql` (**novos/estendidos**); semanticamente: `object/{user,organization,role,session,invitation}.go` | **Modelo de dados de identidade e multi-tenancy (pacote 002 — a MAIOR divergência estrutural do fork, prevista no ADR-0006)**. Novas tabelas pgx: `identity` (UUIDv7, `sub` opaco, campos pessoais cifrados + `email_hash` HMAC, status com terminal R5), `membership` (relação explícita identidade↔organização, R3), `credential` (fatores por IDENTIDADE, forma INV-7: nenhum segredo reversível — TOTP só como `secret_ref` no cofre), `role_assignment` (papéis por MEMBERSHIP, R2), `auth_session` (tenant ativo por sessão, `token_generation` p/ invalidação na troca). Legadas estendidas: `organization`/`role` ganham `id uuid` estável; ~16 tabelas do núcleo ganham `organization_id` (backfill 0010, inventário em `TENANT_INVENTORY.md`). **Isolamento em duas barreiras**: repositórios com escopo obrigatório (tenant/identidade/global — Barreira 1) + RLS FORCE nas tabelas novas com contextos `app.current_organization`/`app.current_identity`/`app.global_read` (Barreira 2), papéis de banco sem BYPASSRLS; travessia e query-sem-predicado quebram o build (`inv5_*`). Mecanismos de migração (inventário/dedup por hash, fusão com aprovação humana obrigatória, credenciais, papéis, ensaio com relatório). **Impacto de triagem**: o modelo `user`=identidade-por-org do upstream ficou SUPERADO — commit do upstream que mude semântica de `user`/`organization`/`role`/`session`/`invitation` exige revisão manual contra o modelo novo (prefixos refletidos em `tools/upstreamwatch/classify.go`) | ADR-0006, RFC-0002, I-6.1..3 | pacote 002 (T-001..T-019) |
| 2026-07-22 | `internal/adapters/{webauthn,totp}/**`, `internal/domain/{credential,recovery,recovery_request,authsession,assurance,operation_catalog,throttle,stuffing,orgmfapolicy,audit_event}.go`, `internal/adapters/postgres/{auth_session_store,org_mfa_policy_store,recovery_request_store,throttle_store,credential_store,factor_remover}.go`, `internal/http/assurance.go`, `internal/migrate/migrations/0022..0026`, `test/invariants/inv8_*` (**novos/estendidos**); semanticamente: `object/{mfa,mfa_totp,webauthn}.go` (MFA legado do Casdoor) | **Fatores e garantia de autenticação (pacote 005, RFC-0006/ADR-0010 — COMPLETO, 20/20)**. O MFA do Casdoor (`object/mfa*.go`, por-`user`, sem teto de nível nem distinção de phishing-resistência) é SUPERADO por um modelo de fator por IDENTIDADE com garantia (AAL) por credencial e TETO por tipo (WebAuthn≤AAL3 hardware/AAL2 passkey, TOTP/recovery≤AAL2, senha≤AAL1; fator acima do teto não é WellFormed). Adaptadores: `webauthn` (cerimônias sobre go-webauthn, só material público INV-7, CloneWarning), `totp` (semente no cofre, cerimônia em 2 passos, fail-closed). Recuperação de uso único + invalidação em massa (`domain/recovery.go`). Sessão ganha `auth_time`/`auth_methods` (migração 0022) → `acr`/`amr` (RFC 8176) DERIVADOS, invariante "AAL comprovado ≤ teto dos métodos". Classificação de operação por nível: `AssuranceLevel` (reusa o de audit_event.go) + `OperationCatalog` + `BuildOperationCatalog` (fonte única) + **INV-8** (`inv8_*` no make invariants: verbo sem classificação/isenção QUEBRA O BUILD). Middleware `internal/http/assurance.go` (guard fail-closed + frescor L1/L2/L3 + desafio RFC 9470 acr_values). Step-up (`AuthSession.StepUp` + SaveStepUp). Política de MFA por org (`OrgMFAPolicy` + migração 0023 + `OrgPolicyAuthority` implementa o port `TenantAuthPolicy`, substitui o provisório) + precedência "mais restritiva vence" no guard e na troca. `enrollment_required` bloqueante (migração 0024). Recuperação com aprovação de pares distintos (migrações 0025, separação de deveres). Rate limit progressivo (migração 0026 `auth_throttle`). Detecção de credential stuffing (in-process, seam do pacote 010). Auditoria de MFA: 8 ações novas no catálogo fechado + `FactorRemover` (remoção L3 auditada fail-closed). **Sem dependência nova** (go-webauthn e pquerna/otp já eram do Casdoor). **Impacto de triagem**: commit do upstream que toque `object/mfa*.go`/`object/webauthn.go` é avaliado contra o modelo de fator novo | ADR-0010, RFC-0006, I-4.1/4.3 | pacote 005 (T-001..T-020, COMPLETO) |
| 2026-07-22 | `internal/domain/audit_*` , `internal/adapters/{postgres/audit_*,auditseal,alerting,wormanchor}/**`, `internal/http/**`, `cmd/audit-verify/**`, `internal/migrate/migrations/0017..0021`, `deploy/postgres/roles.sql` (**novos/estendidos**); semanticamente: `object/record.go` (auditoria legada) | **Subsistema de auditoria imutável (pacote 003, RFC-0003/ADR-0007)**. A `record` legada do Casdoor (tabela comum, editável por quem tem acesso ao banco) é SUPERADA por uma trilha append-only, tamper-evident: `audit_event` (particionada por tempo, encadeada por hash SHA-256 por organização, canonicalização determinística com vetor fixo, `seq` sem lacuna via serialização por tenant), `audit_chain_head`, `audit_seal` (selagem periódica assinada Ed25519 via cofre, verificação após rotação por `key_id`), `audit_seal_anchor` (âncora WORM externa opcional), `audit_event_queue` (modo assíncrono) + `session_event_outbox` (drenado para a cadeia). **INV-2 físico**: papel da app só INSERT/SELECT na auditoria (`roles.sql`) + triggers de bloqueio UPDATE/DELETE/TRUNCATE. Fail-closed (I-5.4): auditoria compõe a transação da operação privilegiada — falha ⇒ negação. Verificador (recomputa cadeia + valida selos + detecta lacunas) como CLI (`cmd/audit-verify`) e endpoint L3 (`internal/http`); verificação diária com alerta crítico; exportação assinada auto-verificável offline por tenant; arquivamento de partição selada + restauração auditada. Instrumentação das mutações administrativas do 002 (o principal viaja no contexto). Selos/cofre/alerta/WORM com impl provisória dev marcada não-produção (reais = OpenBao/observabilidade, pacotes 010+). **Impacto de triagem**: commit do upstream que toque `object/record.go` ou a auditoria é avaliado contra a trilha nova | ADR-0007, RFC-0003, I-5.1..4 | pacote 003 (T-001..T-021) |
