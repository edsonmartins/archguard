# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.swp
/server_*

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/
bin/

.idea/
*.iml
.vscode/settings.json
.claude

tmp/
tmpFiles/
*.tmp
logs/
files/
lastupdate.tmp
commentsRouter*.go

# ignore build result
casdoor
server

# include helm-chart
!manifests/casdoor

web/cypress/videos/
web/cypress/screenshots/

# ArchGuard
.DS_Store

# Artefatos de build gerados
/sbom.json

# Keystore selado do perfil dev (runtime, contém chaves cifradas)
/conf/keystore.sealed
/conf/keystore.sealed.tmp

# Chave privada de token NUNCA versionada (INV-7); gerada no boot + selada no cofre (ADR-0017)
object/token_jwt_key.key
object/token_jwt_key.pem
