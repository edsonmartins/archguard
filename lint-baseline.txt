# Baseline de achados de `go vet` HERDADOS do upstream (design.md 001, nota transitória).
# Formato: "arquivo.go:linha:check" — correspondência EXATA (trava a).
# Entrada obsoleta quebra o build (trava b). Após o fechamento do pacote 001, este arquivo
# SÓ ENCOLHE (trava c). Arquivo tocado por tarefa futura sai do baseline no mesmo commit
# (trava d — boy scout ao tocar, nunca ao ver).
#
# Triagem de 2026-07-20 → entradas removidas em 2026-07-26: obsoletas sob a toolchain
# canônica (go1.25.8 — go.mod/Dockerfile/CI). O `go vet` do 1.25.8 não reporta mais
# `composites` em storage/casdoor.go:9 nem `unreachable` em service/proxy.go:39 (o baseline
# fora gerado sob um Go anterior). Baseline agora VAZIO — trava b (obsoleta quebra o build)
# exigia a remoção; trava c (só encolhe) satisfeita.
